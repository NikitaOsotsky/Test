This task is done using ES5.
To execute the code you need to create `console.log(getAllSMS('Your text'));` for `DOMContentLoaded` callback and run index.html.
The result will be displayed in the console.
